#! /bin/sh

#
# Check Linux 5.8 AArch64 translation with 48-bit virtual addresses
# using data from VMCOREINFO, but with no explicit version.
#

arch=aarch64
ostype=1
osver=0
opts=(
    pagesize=4096
)

. "$srcdir"/xlat-os-common
